{
    "rewrites": [
        {
        "source": "/api/:path*",
        "destination": "https://localhost:8070/:path*"
        },
        {
        "source": "/api/:path*/",
        "destination": "https://localhost:8070/:path*/"
        }
    ]
}